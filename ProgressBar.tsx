# This folder is for static assets served as-is (favicons, images, fonts, etc.)
# Files placed here are copied to the build output root unchanged.
# Currently empty — the app's favicon is inlined as a data URI in index.html.
